// Dashboard.js

import React from 'react';

const Dashboard = ({ selectedCrypto }) => {
  // Customize your dashboard here based on the selected cryptocurrency
  // You can use the 'selectedCrypto' prop to conditionally render content
  return (
    <div className="dashboard">
      {/* Your dashboard components */}
    </div>
  );
};

export default Dashboard;
